#pragma once
#include "data.h"

void bullet_enemy(gameState* game);
void foe_player(gameState* game);
void foe_down(gameState* game);
void miracle_player(gameState* game);
void bullet_miracle(gameState* game);