# 这是洛谷P1065 [NOIP 2006 提高组] 作业调度方案
不会做，读题都得读半天，最后求助gpt  
什么思路：  
根据i,j -> machine  
        -> time  
的关系，用两个[m][n]的数据记录  
所有机器，用[m*n][足够长]数据记录各自的时间轴  
  
在主循环，读入这一步需要操作的器件job，用cnt[job]++的到这一步的工序step(因为全部初始化为0容易，第一个要用的是1，故使用++过后的)然后用job,step通过上面两个数组，获取mac和time  

接着通过finishTime这个数组记录每一个器件上一步结束在时间轴上的时间，设置start来减少搜索时间和更新答案，接着嵌套两个for，找到距离start最近的空余len的时间点，插入  

再更新答案即可  
woc，真nm难