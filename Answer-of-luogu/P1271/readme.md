# 这是洛谷P1271 【深基9.例1】选举学生会
我的第一反应是哈希表，先用数组num来存储输入的数字，通过unoder_mutimap存储键的多个值，就可以实现拿i来输出对应num[i]中的数字。(v1)   
显然有很大的优化:输出的是1、2、3...是排好序的，不一样的是输出的个数，就可以通过存储每一个1、2、3输入的时候的个数，然后一个循环，按个数输出这些数字(v2),或者用num存储数字，从小到大排序一下，输出即可(v3)