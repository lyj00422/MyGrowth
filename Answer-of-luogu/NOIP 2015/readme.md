# 这是NOIP2015 扫地雷
学到了什么：
1.vector<vecotr<char>> map(n,vector(m)) 构造了一个n行m列的数组，想象一下，上边的线是X轴，下边的是Y轴，Y轴上是0~n-1,X是0~m-1,使用构造函数，可以使得全部是“\0",即变量名后（），或者直接vector（），填写元素个数
2.可以创造一个dx，dy的一维矩阵，然后用for循环来简化代码，不用一个一个写,从而扫描出周围的八个
3.还是一边遍历一边输出