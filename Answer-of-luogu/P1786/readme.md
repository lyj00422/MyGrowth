# 这是洛谷P1786 帮贡排序
初看简单，但是自己不知道套路，求助GPT  
首先肯定自己知道创建一个Person类存储信息（由于输入顺序影响排序，需要包含输入顺序id）  
之后，从题目来看，里面设计两个排序，为了直接运用库函数sort，我们需要定义两个bool函数来辅助  
sort(a.beging(),a.end(),bool) bool(b,c)为true的时候，a排在b前面  
把job从字符串映射为int后就可以直接拿来比较，成功定义出两个bool后进入主程序，一个for循环读入，这个过程中分出fixed的，和要排序的normal，先排序normal，并改变job，之后合并，再排序，输出。  
这个过程中要注意for(auto p : all)，这个语法  
严肃学习！